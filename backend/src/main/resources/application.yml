spring:
  datasource:
    url: jdbc:mysql://localhost/alura_foro?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: root123
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true